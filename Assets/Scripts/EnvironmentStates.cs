using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnvironmentStates : MonoBehaviour
{

    public enum SampleStates { StateOne, StateTwo, StateThree };
    public SampleStates mySampleState;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {

        // Check for specific keyboar/mouse input
            // Change mySampleState based on input 
        
    }
}
