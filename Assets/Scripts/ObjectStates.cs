using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObjectStates : MonoBehaviour
{

    public enum SampleStates { StateOne, StateTwo };
    public SampleStates mySampleState;

    float timer = 0f;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {

        // Tick up the timer with Time.deltaTime
            // Change mySampleState based on input 
        
    }
}
